import os
import csv
election results ={}
total_vote=0
lastvote=0
pervotes = []


with open('election_data.csv') as csvfile:
 #   reader = csv.reader(csvfile)
  #  next(csvfile,None)
   # for row in reader:
    #    total_pl=total_pl + int(row[1])
     #     if int(row[1])<0 or lastpl<0:
      #          avgchg.append(int(row[1]) + lastpl)
       #     elif int(row[1])>lastpl:
        #        avgchg.append(int(row[1]) - lastpl)
         #   else:
          #      avgchg.append(lastpl - int(row[1]))
     #   lastpl=(int(row[1]))

#print(len(bankplbudget))
#print(sum(avgchg)/len(avgchg))
#print(max avgchg)




with election_results

